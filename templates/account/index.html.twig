{% extends 'base.html.twig' %}

{% block title %}Beebop - Inscrivez-vous
{% endblock %}

{% block body %}
	<h1 class="form__title">
		Inscrivez-vous !
	</h1>
	{{ form_start(form) }}

	<div class="form__wrapper">
		{{ form_label( form.last_name ) }}
		{{ form_errors( form.last_name ) }}
		{{ form_widget( form.last_name,  { 'attr' : { 'min': 2, 'max': 100 } } ) }}
	</div>

	<div class="form__wrapper">
		{{ form_label( form.first_name ) }}
		{{ form_errors( form.first_name ) }}
		{{ form_widget( form.first_name,  { 'attr' : { 'min': 2, 'max': 100 } } ) }}
	</div>

	<div class="form__wrapper">
		{{ form_label( form.login ) }}
		{{ form_errors( form.login ) }}
		{{ form_widget( form.login,  { 'attr' : { 'min': 3, 'max': 20 } } ) }}
	</div>

	<div class="form__wrapper">
		{{ form_label( form.mail ) }}
		{{ form_errors( form.mail ) }}
		{{ form_widget( form.mail ) }}
	</div>

	<div class="form__wrapper">
		{% for field in form.password %}
			{{ form_label( field ) }}
			{{ form_errors( field ) }}
			{{ form_widget( field,  { 'attr' : { 'min': 8, 'max': 48 } } ) }}
		{% endfor %}
	</div>

	{{ form_widget(form) }}
	<div class="form__login">
		<a href="{{ path('app_login') }}" class="form__link">Retour à la page de connexion</a>
		<button type="submit" class="form__btn">Inscription</button>
	</div>
	{{ form_end(form) }}


{% endblock %}
