{% extends 'base.html.twig' %}

{% block title %}Apiary{% endblock %}

{% block body %}
    <h1>Apiary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ apiary.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ apiary.name }}</td>
            </tr>
            <tr>
                <th>ZipCode</th>
                <td>{{ apiary.zipCode }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ apiary.localisation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_apiary_index') }}">back to list</a>

    <a href="{{ path('app_apiary_edit', {'id': apiary.id}) }}">edit</a>

    {{ include('apiary/_delete_form.html.twig') }}
{% endblock %}
